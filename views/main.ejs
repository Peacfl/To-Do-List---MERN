<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="../style.css">
    <title>To do list</title>

    <script>
        function strike(element) {
            if (element.getAttribute("style") == null)
                element.setAttribute("style", "text-decoration: line-through;");
            else
                element.removeAttribute("style");
        }
    </script>
</head>

<body>
    <div class="row mt-5 mb-3">
        <div class="col-md-3 mx-auto text-center bg-light rounded shadow">
            <h1 class="h1 title text-danger">
                <%= Title %>
            </h1>
        </div>
    </div>
    <div class="row">
        <div class="col-md-3 mx-auto bg-light rounded p-3 shadow">
            <form action="delete" method="post">

                <% List.forEach(element=> { %>
                    <div class="lead text-primary"><input type="checkbox" name="<%=element.name%>"
                            id="<%=element.name%>" onclick='strike(this)'>
                        <%=element.name%>
                    </div>
                    <% }); %>
                        <button name='deleteList' type="submit" value="<%= Title %>" class="btn btn-danger ms-1 mt-3">Clear
                            Items</button>
            </form>

            <form action="add" method="post">
                <div class="row m-1">
                    <input type="text" name="item" placeholder="Add Item" class="input-group-text col-9">
                    <button name='list' type="submit" value="<%= Title %>" class="btn btn-danger ms-2 col-2">+</button>
                </div>
            </form>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>